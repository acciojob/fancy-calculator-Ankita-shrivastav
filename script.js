//your JS code here. If required.
 var firstInput=prompt("Enter the first number:");
var multipliedResult=firstInput*2;

var secondInput=("Enter the second number:");
var finalResult=multipliedResult+parseInt(secondInput);
alert("The final result is:"+ finalResult);
